download install file from
https://www.anaconda.com/distribution/

This gives a *.sh file to install conda python.

Because off size of this *.sh file (> 500 MB). This file is remove from directory, so it is not upload by the git push